// Broadcast
async function broadcast(conn, msg, args, sender) {
    // Envoie un message à tous les groupes / utilisateurs
}

// Stats
async function statsBot(conn, msg, sender) {
    // Affiche les stats du bot (messages, commandes, uptime)
}

// Actif
async function actif(conn, msg, sender) {
    // Affiche les membres les plus actifs avec leurs messages
}