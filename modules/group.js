// Tag all
async function tagAll(conn, msg, sender) {
    // Mentionne tout le monde dans le groupe
}

// Set welcome message
async function welcomeToggle(conn, msg, args, sender) {
    // Active/désactive le message de bienvenue
}