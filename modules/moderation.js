// Gestion des warns
async function warnUser(conn, msg, args, sender) {
    // Ajoute un warn à l'utilisateur
}

// Mute temporaire
async function muteUser(conn, msg, args, sender) {
    // Mute un utilisateur pour X minutes
}

// Ban du bot
async function banUser(conn, msg, args, sender) {
    // Bannir un utilisateur du bot
}